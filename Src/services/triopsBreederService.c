

void triopBreederServiceLoop()
{
    // action every minute

    // get temperature reading 

    // set  heater: h_max - (t_new-t_lower)/(t_target-t_lower)*h_max if t_lower < t_new  < t_target
    // max if t_new < t_lower, 0 if t_new > t_target

    // if time  > t_start and lamp is off: turn lamp on

    // if time > t_end and lamp is on: turn lamp off

    // write temperature, heater value and lamp state to log file
}